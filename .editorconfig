# top-most EditorConfig file
root = true

[*]
# Unix-style newlines with a newline ending every file
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
# Ident with 2 characters wide tabs
indent_style = tab
indent_size = 2
tab_width = 2
# Never wrap lines
max_line_length = off
